{
  "name": "cordia-web-blank",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs",
    "dev:storage": "npm run build && npx wrangler@4.95.0 pages dev public --r2=STORAGE"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
